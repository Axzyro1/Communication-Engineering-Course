#include<stdio.h>
main()
{
	int a;
	int sign(int x);
	while(scanf("%d",&a)!=EOF)
		{
			 {printf("%d\n",sign(a));}
        }
}
int sign(int x)
{
    if(x<0)
	{return -1;}
	if(x==0)
	{return 0;}
	if(x>0)
	{return 1;}
 }