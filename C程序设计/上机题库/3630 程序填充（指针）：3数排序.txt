#include "stdio.h" 
void swap(int *pa,int *pb) 
{ 
int temp; 
temp=*pa; 
*pa=*pb; 
*pb=temp; 
} 
int main() 
{ 
int a,b,c; 
scanf("%d,%d,%d",&a,&b,&c); 
if(a>b) 
swap(&a,&b);
if(a>c) 
swap(&a,&c);
if(b>c) 
swap(&b,&c); 
if(a>c) 
swap(&a,&c); 
printf("%d,%d,%d",a,b,c); 
}