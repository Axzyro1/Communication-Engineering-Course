#include <stdio.h> 
void swap(int *p1,int *p2) 
{ 
int t;
t=*p1;
*p1=*p2;
*p2=t;
} 
int main() 
{ int a,b; 
int *pointer_1,*pointer_2; 
while(scanf("%d%d",&a,&b)!=EOF) 
{ 
pointer_1=&a;
pointer_2=&b; 
if(a < b) 
{ 
swap(pointer_1,pointer_2);
} 
printf("%d %d\n",a,b); 
} 
} 

