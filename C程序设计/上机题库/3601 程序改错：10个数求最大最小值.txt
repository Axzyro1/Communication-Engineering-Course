#include <stdio.h>
int main()
{ float x,max,min; int i;
/******** 1 *******/
for(i=1;i<=10;i++) {
scanf("%f",&x);
/******* 2 ********/
if(i==1) { max=x;min=x;}
if(x>max) max=x;
if(x<min) min=x;
 }
printf("%f,%f\n",max,min);
}