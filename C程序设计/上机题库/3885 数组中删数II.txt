3885 Problem E 零起点学算法84——数组中删数II 
#include<stdio.h>
int main()
{
	int i=0,n,m,a[i];
	while(scanf("%d",&n)!=EOF)
	{
		for(i=0;i<n;i++)
		{
			scanf("%d",&a[i]);
		}
		scanf("%d",&m);
		for(i=0;i<n;i++)
		{
			if (m!=a[i])
			printf("%d ",a[i]);
		}
		printf("\n");
	}
}