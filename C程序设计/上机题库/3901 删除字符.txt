#include <stdio.h>
#include <string.h>
int main()
{
    char c[1000],m;
    int len, i;
    while(gets(c)!=NULL) 
    {
    	scanf("%c",&m); 
        getchar();
        for (i=0;c[i]!='\0'; i++)
        {
    	if(c[i]!=m)
        printf("%c",c[i]);
        }
        printf("\n");
    }
}