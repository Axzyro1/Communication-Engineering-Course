3522 Problem H ËØÊýÇóºÍ 
#include<stdio.h>
int main()
{
    int i,j,m,n,s;
    while(scanf("%d",&n)!=EOF)
    {
    	s=0;
    	for(i=1;i<=n;i++)
    	{
		scanf("%d",&m);
		if(m==1)
	    m=m;
		else
        for(j=2;j<m;j++)
        {
		if(m%j==0)
        break;
        }
        if(m==j)
	    {
		printf("%d ",m);
        s+=m;
	    }
        else
        s=s;
    	}
	    printf("s=%d\n",s);
    }
}
