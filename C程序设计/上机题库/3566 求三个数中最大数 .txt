#include<stdio.h>
main()
{
	int a,b,c;
	int max(int x,int y,int z);
	while(scanf("%d %d %d",&a,&b,&c)!=EOF)
		{
         printf("%d\n",max(a,b,c));	
		}
}
int max(int x,int y,int z)
{
	int i;
	if(x>y)
	{i=x;}
	else
	{i=y;}
	if(i>z)
	{return i;}
	else
	{return z;}
}