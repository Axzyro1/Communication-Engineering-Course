#include<stdio.h>
#include<string.h>
int length(char *s[])
{   
    int i=0;
    while(*s[i]!='\n')
	i++;
}
int main()
{
    char s[80];
    while(gets(s)!=NULL)
    {   int b;
        b=strlen(s);
        printf("%d\n",b);
    } 
}
