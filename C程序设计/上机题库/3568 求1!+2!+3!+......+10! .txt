#include<stdio.h>
int main(void)
{
    int i,x;
    double s;
    int sum(int n);
    s=0;
    for(i=1;i<=10;i++)
		{
        s=s+sum(i);
        }
    printf("%.6f\n",s);
}
 int sum(int n)
{
    int s,i;
    s=1;
    for(i=1;i<=n;i++)
		{
        s=s*i;
        }
    return s;
}