#include <stdio.h>
#include <math.h>
int main()
{
int a,b,c;
scanf("%d%d%d",&a,&b,&c);
if((a>b&&a>c&&(a*a==b*b+c*c))||(b>a&&b>c&&(b*b==a*a+c*c))||(c>b&&c>a&&(c*c==b*b+a*a)))
{printf("yes\n");}
else
{
	printf("no\n");
}
} 