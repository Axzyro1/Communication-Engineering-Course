 3570 Problem G 函数调用，统计数中'2’的个数 
#include<stdio.h>
main()
{
	int n;
	int countdigit(int x);
	while(scanf("%d",&n)!=EOF)
		{
		printf("%d\n",countdigit(n));	
		}
}
int countdigit(int x)
{
	int i=0,s,m;
	m=x;
	for(;m>0;)
	{
	s=m%10;
	m=m/10;
	if(s==2)
	{i++;}
	}
	return i;
}