#include <stdio.h>
#include <math.h>
long f(long n)
{  long m,y=0;  m=fabs(n);
   while(m!=0) { 
     y=y*10+m%10; 
     m=m/10;
   }
   if(n>=0) return y;
   else return -y ;
}
int main()
{
	int m;
	while(scanf("%d",&m)!=EOF)
	{printf("%d\n",f(m));}
}