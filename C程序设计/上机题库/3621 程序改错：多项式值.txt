#include<stdio.h> 
#include<math.h> 
int main() 
{ float x,eps,s=1,t=1,i=1; 
scanf("%f%f",&x,&eps); 
do { 
/***** 1 *****/ 
t=-t*x/i++; 
s+=t; 
/***** 2 *****/ 
} while(fabs(t)>=eps);
 printf("%f\n",s); 
}