#include<stdio.h>
#include<math.h>
main()
{
 double x,y;
 int z;
 scanf("%lf",&x);
 if(x<=0) z=0;        
 if(x>0&&x<10) z=1;
 switch(z)
 {
   case 0:   y=pow(x, 5) + 2 * x + 1;;  break;
   case 1:   y=exp(x) - log10( x ); break;
 }
 printf("y=%.3f\n",y);
}