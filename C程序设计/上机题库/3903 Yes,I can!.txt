#include <stdio.h>
#include <string.h>
int main()
{
    char c[1000];
    int len, i;
    gets(c); 
    {
    len = strlen(c);
    printf("I am ");
    for (i=0; i<len; i++)
    {
        printf("%c", c[i]);
    }
    printf(",yes,I can!");
    printf("\n");
    }
}