#include <stdio.h>
#include <string.h>
int main()
{
    char str[80],m;
    int len,i,j,t;
    gets(str);
    {
		len=strlen(str);
        for (i=0;i<len-1; i++)
        {
        	for(j=0;j<len-1-i;j++)
        	{
       		if(str[j]>str[j+1])
        	{t=str[j];
			str[j]=str[j+1];
			str[j+1]=t;
        	}
        	}
        }
        for (i=0;i<len;i++)
		{
			printf("%c",str[i]);	
		}
        printf("\n");
    }
}